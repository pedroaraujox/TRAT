# PostgreSQL Configuration
DB_PASSWORD=CHANGE_ME_SECURE_DB_PASSWORD_32_CHARS_MIN

# Security Tokens (use strong random values)
# Generate with: openssl rand -base64 32
AGENT_TOKEN=CHANGE_ME_SECURE_AGENT_TOKEN_MIN_32_CHARS
ADMIN_TOKEN=CHANGE_ME_SECURE_ADMIN_TOKEN_MIN_32_CHARS

# Bootstrap Admin User
ADMIN_EMAIL=admin@outboxtech.com.br
ADMIN_PASSWORD=CHANGE_ME_SECURE_ADMIN_PASSWORD_MIN_12_CHARS

# Optional: SMTP Configuration (leave empty if not using)
# SMTP_ENABLED=false
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_FROM=backup@outboxtech.com.br
